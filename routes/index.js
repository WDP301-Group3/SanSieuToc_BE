const express = require('express');
const router = express.Router();

// Import routes here
// router.use('/api/managers', require('./manager.routes'));
// router.use('/api/categories', require('./category.routes'));
// etc...

module.exports = router;
